apiVersion: comcast.github.io/v1
kind: KuberhealthyCheck
metadata:
  name: dns-status-internal
  namespace: kuberhealthy
spec:
  runInterval: 2m
  timeout: 15m
  podSpec:
    serviceAccountName: dns-internal-check
    containers:
      - env:
          - name: HOSTNAME
            value: "hostnames.default.svc.cluster.local"
          - name: NAMESPACE
            value: "kube-system"
          - name: DNS_NODE_SELECTOR
            value: apps=k8s-dns
          - name: NODE_NAME
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
        # image: kuberhealthy/dns-resolution-check:v1.4.3
        image: omegavveapon/kuberhealthy-internal-dns-check:working-check
        imagePullPolicy: Always
        name: main
        resources:
          requests:
            cpu: 10m
            memory: 50Mi
